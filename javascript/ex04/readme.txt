Create a function called firstAndLastCharacter that take a string with more than two characters as a single parameter, then returns the string with the first and last character removed
You must create a program.js file that contains your function
